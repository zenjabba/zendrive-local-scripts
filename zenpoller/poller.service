[Unit]
After=docker.service mergerfs.service

[Service]
ExecStart=/opt/scripts/zendrive/zenpoller/start_poller.sh
ExecStop=/usr/bin/killall start_poller.sh
Restart=on-abort
RestartSec=5
StartLimitInterval=60s
StartLimitBurst=3

[Install]
WantedBy=default.target
